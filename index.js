const randomNum1 = Math.floor(Math.random() * 6) + 1;

const randomDiceImage = 'dice' + randomNum1 + '.png';

const randomImage;

document.querySelectorAll;

const randomNum2 = Math.floor(Math.random() * 6) + 1;
